import java.util.*;

public class security2 {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int numCases = input.nextInt();
        for (int currCase = 0; currCase < numCases; currCase++) {
            int totalRooms = input.nextInt();
            int a = input.nextInt();
            int b = input.nextInt();
            int[] roomNums = new int[totalRooms];
            int[][] adj = new int[totalRooms][totalRooms];
            for (int i = 0; i < totalRooms; i++)
                roomNums[i] = input.nextInt();

            for (int i = 0; i < totalRooms; i++) {
                for (int j = 0; j < totalRooms; j++) {
                    adj[i][j] = a*()
                }
            }
        }
    }
}
